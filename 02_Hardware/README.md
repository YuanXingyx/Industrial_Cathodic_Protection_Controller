# Hardware

This directory owns low-voltage module requirements, design notes and tests. No complete schematic is claimed at initialization. Values, parts and performance are `TBD` unless backed by a measurement or cited requirement. Each module must pass its own test gate before PCB integration.
