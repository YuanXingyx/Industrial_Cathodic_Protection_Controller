# Reference Input Design

Phase 1 directly validates the MCU ADC with a safe potentiometer signal. Future AFE candidates must be evaluated for high input impedance, low input bias, bipolar translation, bandwidth/noise and non-loading protection. Circuit values and op amp selection: TBD.
